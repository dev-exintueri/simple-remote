# simple-remote

가족·지인의 Windows PC 를 원격으로 돕는 P2P 원격 제어 프로그램이다. Rust + WebRTC(TURN 없음) 로 만들고, 연결 시작용 signaling 서버는 Cloudflare Workers(TypeScript) 에 둔다.

## 먼저 읽을 문서

| 문서 | 내용 |
|---|---|
| `docs/superpowers/progress/2026-09-23-remote-control-brainstorming.md` | 현재 진행 상태, 다음 할 일, 결정 기록 D1~D20. 맨 앞의 "클라우드 세션 인계" 절부터 읽는다 |
| `docs/superpowers/specs/2026-09-24-simple-remote-sp1-design.md` | 승인된 SP1 설계 spec. 구현의 기준 |
| `docs/research/` | spec 의 근거 조사 3건 (TeamViewer 기능, P2P·보안, Windows/macOS host 플랫폼) |

- 진행 기록은 단계가 끝날 때마다 갱신한다: 어디까지 했는지, 다음에 무엇을 할지, 어떤 결정을 왜 내렸는지. 다음 세션이 이 파일만 읽고 이어받을 수 있어야 한다.
- 설계를 바꾸는 결정은 spec 과 진행 기록의 결정 기록에 함께 남긴다.

## 작업 절차

- 새 기능이나 동작 변경은 설계 승인 → spec → 구현 계획 → 구현 순서로 진행한다. 사람이 승인한 단계를 건너뛰지 않는다.
- superpowers plugin 이 있으면 그 skill(brainstorming, writing-plans, subagent-driven-development 등)을 쓴다. 없으면 같은 산출물을 같은 위치에 만든다: spec 은 `docs/superpowers/specs/`, 구현 계획은 `docs/superpowers/plans/YYYY-MM-DD-<이름>.md`. 계획의 각 task 는 파일 목록, 실패하는 테스트, 최소 구현, 통과 확인, commit 단계를 실제 코드와 명령으로 적는다.
- 구현 계획 형식
  - 맨 앞: 목표 한 문장, 접근 방식 2~3문장, 사용 기술, spec 경로, spec 에서 옮긴 전역 제약(정확한 값).
  - task 마다: 파일 목록, 다른 task 와 주고받는 이름·타입, 실패하는 테스트 코드, 실행 명령과 기대 결과, 최소 구현 코드, 통과 확인, commit 단계.
- 계획에 "TBD", "나중에", "적절히 처리" 같은 빈칸을 두지 않는다. 다 쓴 뒤 spec 의 모든 요구가 task 로 덮였는지 스스로 점검한다.
- 구현은 TDD 로 한다. 테스트를 먼저 쓰고 실패를 확인한 뒤 구현한다.
- 완료나 통과를 말하기 전에 해당 명령을 실제로 실행하고 출력으로 확인한다.
- 버그나 실패는 원인을 확인한 뒤 고친다. 추측으로 고치지 않는다.
- 라이브러리 API, 버전, flag, 지원 여부는 추측하지 않는다. 문서, 소스, 타입 정의를 읽고 확인한 뒤 쓴다.
- Windows 실행이 필요한 검증(화면 캡처, 입력 주입, SYSTEM 권한, 설치)은 Linux 환경에서 할 수 없다. 검사 코드와 실행 방법을 준비하고 사람에게 실행을 요청한다.

## 구현 원칙

- 현재 요구를 완전히 만족하는 가장 단순한 구현을 고른다. 추측성 추상화, 설정, 간접 계층을 만들지 않는다.
- 쓰지 않게 된 코드 경로는 호환 shim 을 덧대지 말고 지운다. 지우기 전에 호출자가 없는지 확인한다.
- 작게 끝까지 동작하는 것부터 만들고 기능을 층층이 더한다. 동작하는 제품을 미완성 복잡도와 바꾸지 않는다.
- 나중에 갈아엎을 것을 이미 아는 구조는 받지 않는다.
- 필요한 기능은 이미 쓰는 의존성 → 잘 관리되는 라이브러리 → 직접 구현 순서로 찾는다.
- 보안 경로는 실패하면 막는다(fail closed). 암호화 없이 진행하는 코드 경로를 두지 않는다.

## 사람과 대화하는 방식

- 한국어로 답한다. 기술 용어와 코드 식별자는 원래 형태로 쓴다.
- 한자어 직역 말투를 쓰지 않는다. 한자어 대신 차라리 영어를 쓴다.
- 이모지와 em-dash 를 쓰지 않는다. 아첨하는 서두나 맺음말을 쓰지 않는다.
- 사람이 되묻지 않고 판단할 수 있게 설명한다.
  - 아직 없는 파일·명령을 말할 때는 같은 문장에 "신규 작성 예정"을 밝힌다.
  - 비용·영향은 사람이 체감하는 단위로 바꿔 말한다 (시간, 건수 등).
  - 사람이 모를 수 있는 용어는 처음 쓸 때 한 줄 풀이를 붙인다.
  - 결정 시점에는 흩어진 배경을 그 자리에 다시 모아 보여 준다.
  - "무엇을 해야 하나"에는 사람이 할 일과 에이전트가 할 일을 나눠 답한다.
  - 결정이나 선택을 제시할 때는 근거를 같은 자리에 쓴다.
- 사람이 "~하면 어떨까", "~도 가능할까"라고 말하면 실행 지시로 받지 않는다. 타당성, 부작용, 대안을 평가해 의견을 말하고, 명시적으로 요청받은 뒤에 실행한다.
- 선택지 질문(AskUserQuestion)은 한 번에 하나씩, 선택지마다 결과와 비용을 풀어서 쓴다. 추천안이 있으면 첫 번째에 두고 "(추천)"을 붙인다.
- Artifact 도구는 쓰지 않는다. 결과물은 텍스트, 마크다운, 파일로만 낸다.

## git 규칙

- 기본 브랜치는 `develop` 이다. `develop` 반영은 PR 로 한다. `master` 는 배포 기록용이며 release / hotfix 브랜치 머지로만 갱신한다.
- release 브랜치는 `release/YYYYMMDD`, hotfix 는 `hotfix/YYYYMMDD-p{순서}` 이다. 마무리할 때는 master 머지 후 annotated tag(브랜치 이름에서 prefix 를 뺀 값, 메시지 비움)를 만들어 함께 push 한다. master push 와 tag push 전에는 사람에게 확인받는다.
- `git add` 는 파일을 하나씩 지정한다 (`.`, `-A` 금지). `.env` 나 인증 정보는 올리지 않는다.
- commit 메시지 형식 (HEREDOC 으로 전달):

```
{type}: 제목

## 배경
(왜 이 변경이 필요한지)

## 주요 변경사항
- (정책·조건 변경은 빠짐없이. 코드 수준 변경은 git diff 로 갈음)

Co-Authored-By: {모델 이름} <noreply@anthropic.com>
```

- type: feat | fix | refactor | docs | test | chore. 본문은 한국어로 쓴다.
- 주석, commit 메시지, 코드 식별자에 시간이 지나면 의미가 사라지는 값(임시 순번, 세션 ID, "방금", "이번에" 같은 표현)을 넣지 않는다.
- PR 본문에는 `Generated with Claude Code` 문구를 넣지 않는다. commit 의 `Co-Authored-By` 는 유지한다.
